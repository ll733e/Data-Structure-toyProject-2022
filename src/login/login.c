#include <stdio.h>

void add_user(char* ID, char* pw){
	//fwrite();
	//fclose();
}

void check(char* ID, char* pw){
	//if(ID==NULL) retrun -1;
}

void del_user(char* ID, char* pw){
	//remove(ID.json);
}

